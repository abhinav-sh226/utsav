installed virtual environment using python -m venv
activate the virtual env
pip is a python package manager, user command pip install -r requirements.txt
slecet interpretor in editor in vscode(Ctrl + Shift + P)
application will be a fastAPI application